let header = document.querySelector("h1");
console.log("テスト");

/*
document.addEventListener("DOMContentLoaded", function () {
    let header = document.querySelector("h1");
    header.textContent = "Hello world!";
});
/*
let header = document.querySelector("h1");
//let：変数の宣言
//document：JavaScriptでウェブページの文書構造を操作するためのオブジェクト。
//querySelector()：引数に指定されたCSSセレクターに一致する最初の要素を取得する。
header.textContent = "Hello world!";
//textContent：指定したテキスト内容を変更。この場合はheaderに格納されたh1タグ。
*/

/*
function keisan(num) {
    // 引数 * 3をculcに代入する
    let culc = num * 3;
    // culcが9の倍数だったら1を加算する
    if (culc % 9 === 0) {
        culc += 1;
    }
    return culc;
}

let num = 2;
for (let i = 0; i < 10; i++) {
    num = keisan(num);
}
*/
